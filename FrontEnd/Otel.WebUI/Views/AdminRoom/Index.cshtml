@model List<ResultRoomDTO>

@{
    ViewData["Title"] = "Danh sách phòng";
    Layout = "~/Views/AdminLayout/_AdminLayout.cshtml";
}

<div class="container mt-4">
    <h2 class="text-center mb-4">Quản lý phòng</h2>

    <div class="mb-3">
        <a href="@Url.Action("AddRoom", "AdminRoom")" class="btn btn-primary">Thêm phòng mới</a>
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-warning text-center">
            Không có phòng nào được hiển thị.
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-bordered table-striped text-center" style="width: 100%;">
                <thead class="table-dark">
                    <tr>
                        <th>Hình ảnh</th>
                        <th>Tiêu đề</th>
                        <th>Số phòng</th>
                        <th>Giá</th>
                        <th>Số giường</th>
                        <th>Số phòng tắm</th>
                        <th>Wifi</th>
                        <th>Mô tả</th>
                        <th>Hành động</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var room in Model)
                    {
                        <tr>
                            <td>
                                <img src="~/hotel-html-template/img/@room.RoomCoverImage" alt="Room Image" style="width: 80px; height: auto;" />
                            </td>
                            <td>@room.Title</td>
                            <td>@room.RoomNumber</td>
                            <td>@room.Price</td>
                            <td>@room.BedCount</td>
                            <td>@room.BathCount</td>
                            <td>@room.Wifi</td>
                            <td>@room.Description</td>
                            <td>
                                <a href="@Url.Action("UpdateRoom", "AdminRoom", new { id = room.RoomId })" class="btn btn-warning btn-sm">Chỉnh sửa</a>
                                <a href="@Url.Action("DeleteRoom", "AdminRoom", new { id = room.RoomId })" class="btn btn-danger btn-sm" onclick="return confirm('Bạn có chắc chắn muốn xóa phòng này không?');">Xóa</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }

    <div class="mt-4 text-center">
        <p class="text-muted">Tổng số phòng: <strong>@Model.Count()</strong></p>
    </div>
</div>

<style>
    .table {
        margin-top: 20px;
        border-collapse: collapse;
    }

    .table th, .table td {
            vertical-align: middle;
            text-align: center;
        }

    .btn-warning {
        border-color: #ffc107;
    }

    .btn-danger {
        border-color: #dc3545;
    }
</style>
