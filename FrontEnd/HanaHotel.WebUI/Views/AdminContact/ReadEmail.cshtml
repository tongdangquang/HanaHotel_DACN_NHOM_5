@model DetailContactDTO

@{
    ViewData["Title"] = "Đọc Email";
    Layout = "~/Views/AdminLayout/_AdminLayout.cshtml";
}

<div class="content-body">
    <div class="row page-titles mx-0">
        <div class="col p-md-0">
            <ol class="breadcrumb">
                <li class="breadcrumb-item">
                    <a href="/AdminContact/Inbox">
                        <i class="fa fa-arrow-left" aria-hidden="true" style="margin-right : 5px"></i> Hộp thư đến
                    </a>
                </li>
            </ol>
        </div>
    </div>

    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        @await Component.InvokeAsync("_ContactSidebarPartial")
                        <div class="email-right-box">
                            <div class="read-content">
                                <hr>
                                <div class="media mb-4 mt-1">
                                    <div class="media-body">
                                        <span class="float-right text-muted">@Model.Date.ToString("HH:mm")</span>
                                        <h4 class="m-0 text-primary">@Model.Subject</h4>
                                        <small class="text-muted">Người gửi: @Model.Email</small>
                                    </div>
                                </div>
                                <h5 class="mb-3">Xin chào, @Model.Name,</h5>
                                <p>@Model.Message</p>
                                <hr>
                            </div>
                            <div class="text-right">
                                <a href="@Url.Action("SendEmail", "AdminContact", new { id = Model.ContactID })" class="btn btn-primary mb-3">Trả lời</a>
                                <a href="@Url.Action("DeleteEmail", "AdminContact", new { id = Model.ContactID })"
                                   class="btn btn-danger mb-3"
                                   onclick="return confirm('Bạn có chắc chắn muốn xóa email này không?');">
                                    Xóa
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
