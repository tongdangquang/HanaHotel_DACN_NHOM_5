@model List<HanaHotel.EntityLayer.Concrete.Role>

@{
    ViewData["Title"] = "Quản lý vai trò";
    Layout = "~/Views/AdminLayout/_AdminLayout.cshtml";
}

<div class="container mt-4">
    <h2 class="text-center mb-4">Quản lý vai trò</h2>

    <div class="mb-3">
        <a href="@Url.Action("AddRole", "AdminRole")" class="btn btn-primary">Thêm vai trò mới</a>
    </div>

    <div class="table-responsive">
        <table class="table table-hover table-striped">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Tên vai trò</th>
                    <th scope="col" class="text-center">Hành động</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var role in Model)
                    {
                        <tr>
                            <td>@role.Id</td>
                            <td>@role.Name</td>
                            <td class="text-center">
                                <a href="@Url.Action("UpdateRole", "AdminRole", new { id = role.Id })" class="btn btn-warning btn-sm">Sửa</a>
                                <a href="@Url.Action("DeleteRole", "AdminRole", new { id = role.Id })" class="btn btn-danger btn-sm" onclick="return confirm('Bạn có chắc muốn xóa vai trò này?');">Xóa</a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="3" class="text-center">Chưa có vai trò nào.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success" role="alert">
            @TempData["Success"]
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger" role="alert">
            @TempData["Error"]
        </div>
    }
</div>
